# Layout components

Esta carpeta queda reservada para wrappers y piezas de layout reutilizables.

Nota: por compatibilidad actual, `NavBar` y `Sidebar` siguen en `src/layouts/` hasta su migracion gradual.
